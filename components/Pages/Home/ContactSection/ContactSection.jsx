import classNames from "classnames";
import React from "react";

export default function ContactSection() {
	return (
		<section className={classNames("home-section")}>
			<div className={classNames("container")}>h4</div>
		</section>
	);
}
